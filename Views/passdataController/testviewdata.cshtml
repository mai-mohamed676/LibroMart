@model passdataController
@{
    ViewData["Title"] = "testviewdata";
}

<h1>testviewdata</h1>

/*<h2>@Model.Name</h2>
<h3>message:@ViewData["message"]</h3>
<select class="form-control">
    @foreach(var item in(List<string>)ViewData["brch"] ){
        <option>@item</option>
        }
</select>
@((int)ViewData["Temp"]+10)*/